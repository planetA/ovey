# this file is only here in order for CLion IDE to work properly
# I don't use Cmake in this project. For example CLion displays
# the correct project name in the project manager instead of just
# the directory name.

cmake_minimum_required(VERSION 3.0)
project("Ovey userland tools (Rust)")

